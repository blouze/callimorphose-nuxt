query Ecriture ($slug: String!) {
  ecritureBySlug (slug: $slug) {
    id, name, description, priority, slug,
    image { id, formats },
    realisations { id, title,
      images { formats }
    },
    prev { id, name, slug },
    next { id, name, slug }
  }
}